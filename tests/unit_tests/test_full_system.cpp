#include "../../tests/simulator.hpp"

// --AHMED-- (do this one first)
TEST(run_simulator) {
    // create a simulator object
    // start the simulator (it may do this automatically)
    // allow it to run for a bit
    // end the simulator
    // check to make sure all of these things works
    ASSERT_TRUE(true);
}

// --AHMED--
TEST(add_image_to_chain) {
    // create a simulator and follow steps like above
    // call the simulator function to pass input to a node
    // check the blockchain files to make sure it propagate through the network
    ASSERT_TRUE(false);
}