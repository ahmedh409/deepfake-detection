// node.h
#include <cstdint>

class Node {
public:
    
    Node(uint32_t id);

private:
    // form of identification
    uint32_t _id;

    // need to implement some sort of digital signature scheme
    // here
};